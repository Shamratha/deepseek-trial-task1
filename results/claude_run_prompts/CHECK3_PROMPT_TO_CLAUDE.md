# Prompt to give Claude (Check 3 — full end-to-end task)

You are a senior coding agent. Implement, in this workspace, the full task required by the attached 'task.md' (or the brief/lock data provided). Do not stop at a summary: produce the complete deliverable package exactly as the task requires, run any provided verifier or self-check, record timing, and fix failures until the verifier passes.

Use the workspace exclusively. Do not create files outside the workspace. If credentials or environment configuration are required and not present, report exactly which are missing and stop.

Deliver at the end: path(s) to the produced artifact(s), the exact verification result (or evidence), and total elapsed time.
